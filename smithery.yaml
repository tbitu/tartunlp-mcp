runtime: "python"
startCommand:
  type: "http"
  configSchema:
    type: "object"
    properties:
      timeout:
        type: "integer"
        title: "Request Timeout"
        description: "Request timeout in milliseconds"
        default: 5000
        minimum: 1000
        maximum: 30000
  exampleConfig:
    timeout: 5000
